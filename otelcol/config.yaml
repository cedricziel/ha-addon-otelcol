# https://developers.home-assistant.io/docs/add-ons/configuration#add-on-config
name: OpenTelemetry collector
version: "0.6.0.53"
slug: otelcol
description: Bring the OpenTelemetry collector into your home
url: "https://github.com/cedricziel/ha-addon-opentelemetry-collector"
ports:
  4317/tcp: 4317
  4318/tcp: 4318
ports_description:
  4317/tcp: OTLP/gRPC
  4318/tcp: OTLP/HTTP
arch:
  - aarch64
  - amd64
init: false
map:
  - share:rw
  - config:rw
watchdog: http://[HOST]:13133/
apparmor: true
options:
  config: |
    receivers:
      otlp:
        protocols:
          grpc:
          http:
    processors:
      batch:
    exporters:
      logging:
    extensions:
      health_check:
      pprof:
      zpages:
    service:
      extensions: [health_check,pprof,zpages]
      pipelines:
        traces:
          receivers: [otlp]
          processors: [batch]
          exporters: [logging]
        metrics:
          receivers: [otlp]
          processors: [batch]
          exporters: [logging]
        logs:
          receivers: [otlp]
          processors: [batch]
          exporters: [logging]
schema:
  config: "str"
image: "ghcr.io/cedricziel/{arch}-ha-addon-otelcol"
